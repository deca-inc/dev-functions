{
  "name": "your-project-name",
  "scripts": {
    "dev": "tsx watch src/localServer.ts",
    "build": "node build.js",
    "launch": "pnpm build && doctl serverless deploy .",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.8.7",
    "dotenv": "^16.4.5",
    "esbuild": "^0.24.0",
    "express": "^4.21.1",
    "tsx": "^4.19.2",
    "typescript": "^5",
    "vitest": "^2.1.4"
  }
}
